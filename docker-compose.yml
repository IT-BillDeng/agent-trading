# ============================================================
# Agent Trading — Docker 编排
# ============================================================
# 主服务：dashboard（Web 看板 + 内置信号调度器）
# ============================================================

services:
  # ---------- 主服务：Dashboard + 内置调度器 ----------
  dashboard:
    build:
      context: .
      dockerfile: dashboard/Dockerfile
    container_name: agent-dashboard
    ports:
      - "8088:8088"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
      - ./rules:/app/rules
      - ./news:/app/news
      - ./properties:/app/properties
      - ./logs:/app/logs
      - ./runtime/engine:/app/runtime
    environment:
      - TZ=Asia/Shanghai
      - ENGINE_CONFIG_DIR=/app/config
      - ENGINE_RULES_DIR=/app/rules
      - ENGINE_NEWS_DIR=/app/news
      - TIGER_PROPERTIES_DIR=/app/properties
      - ENGINE_WORKSPACE=/app
      - ENGINE_WATCHLIST_PATH=/app/data/watchlist.json
      - ENGINE_QUOTE_PROVIDER=yfinance
      - ENGINE_LOGS_DIR=/app/logs
      - ENGINE_RUNTIME_DIR=/app/runtime
      - ENGINE_APP_CONFIG=/app/config/app_config.docker.json
      # 调度器配置
      - ENGINE_SCHEDULER_PROVIDER=yfinance
      - ENGINE_SCHEDULER_INTERVAL=60
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8088/health')"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

