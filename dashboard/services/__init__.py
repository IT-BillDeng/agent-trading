"""Dashboard service layer.

This package centralizes dashboard-side orchestration around engine adapters so
the dashboard no longer maintains a second implementation of broker and market
data clients.
"""

